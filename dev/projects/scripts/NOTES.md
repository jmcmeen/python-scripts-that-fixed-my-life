# NOTES

This is the folder. `scripts/` inside `projects/` inside `dev/`.

`the_script.py` used to live here. It has since been promoted to a
production-stable catalog entry (#44,999) and now ships from the package, where
it manages its own working directory because this folder could not be relied
upon.

This folder has not been opened since March.

It is now June. I am only here because I was writing a README about it.

I am going to close my laptop.
